<h1>Players</h1>
<div id="player-list">
  <ul class="list">
    <% @players.each do |player| %>
    <li>
      <%= link_to(player.name, "/player/#{player.url_name}", { class: "player-name" }) %> (<%= player.url_id %>)
    </li>
    <% end %>
  </ul>
</div>
